{
  "name": "job-tracker-server",
  "version": "1.0.0",
  "description": "Express + MongoDB backend for the Job Application Tracker",
  "main": "server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.9.0"
  }
}
